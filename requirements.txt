# Librerías a instalar para el entorno virtual

# Librerías para RAG
google-genai >= 1.50.1

# Streamlit & co
chainlit >= 1.0.0
python-dotenv >= 1.0.0
streamlit >= 1.54.0
langchain-community
pypdf >= 6.7.0

# Soporte para notebooks, por si acaso
ipykernel